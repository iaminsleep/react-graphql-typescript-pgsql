query GetPosts($limit: Int!, $cursor: String) {
    posts(limit: $limit, cursor: $cursor) {
        posts {
            id
            title
            createdAt
            updatedAt
            creatorId
            textSnippet
        }
        hasMore
    }
}
