mutation UpdatePost($id: Int!, $file: Upload, $text: String!) {
    updatePost(id: $id, file: $file, text: $text) {
        id
        createdAt
        updatedAt
        image
        text
        textSnippet
    }
}
