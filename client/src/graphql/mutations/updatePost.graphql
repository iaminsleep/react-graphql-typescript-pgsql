mutation UpdatePost($id: Int!, $image: String, $text: String!) {
    updatePost(id: $id, image: $image, text: $text) {
        id
        createdAt
        updatedAt
        text
        textSnippet
    }
}
