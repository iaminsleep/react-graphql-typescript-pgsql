# We don't need to specify fields inside options, which is very convinient. All the options are stored inside UsernamePasswordInput which is stored in

mutation Register($options: UsernamePasswordInput!) {
    register(options: $options) {
        ...RegularUserResponse
    }
}
